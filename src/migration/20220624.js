
const index = {
    migrateUp: async (conn) => {
        console.info(`Migrate up...`);
    },
    migrateDown: async (conn) => {
        console.info(`Migrate down...`);
    },
};

export default index;
